# BIGDATA

---

### Tài liệu Dự án: `/docs`

## Cấu trúc thư mục
* `init/docker-compose.yml` → `postgres` service    
* `init/docker-compose.yml` → `kafka` + `zookeeper`             
* `init/docker-compose.yml` → `mongodb`         
* `init/docker-compose.yml` → `minio`        
* `init/docker-compose.yml` → `debezium-connect`
* `spark-streaming/`       
* `spark-batch/`         
* `SpringBoot/`        
* `airflow` (if present)   
* `prometheus` (if present)   
* `grafana` (if present) 
